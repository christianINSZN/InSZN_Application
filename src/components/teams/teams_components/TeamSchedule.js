// In src/components/teams/teams_components/TeamSchedule.js
import React from 'react';

const TeamSchedule = ({ teamData, year }) => {
  return (
    <div>
      <h2 className="text-lg font-semibold mb-2">Schedule</h2>
      {/* Add schedule table based on teamData later */}
      <p>Placeholder for schedule data</p>
    </div>
  );
};

export default TeamSchedule;